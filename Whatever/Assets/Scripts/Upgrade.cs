using TMPro;
using UnityEditor;
using UnityEngine;
using UnityEngine.UI;

/*public class Upgrade : MonoBehaviour
{
    private Chest ChestInv;

    private Money MoneyScript;

    public GameObject Slot1;

    //public GameObject Slot2;

    public Button Upgrader;

   //public TextMeshProUGUI MoneyText;

   // public float Money = 100f;

    public GameObject Slot;

    //public Pickup Pickup;

    public GameObject Placeholder;

    // Start is called before the first frame update
    void Start()
    {
        Slot1.SetActive(false); 
        
        //Slot2.SetActive(false);

        Upgrader.interactable = true;

        ChestInv = GameObject.FindGameObjectWithTag("Chest").GetComponent<Chest>();

        MoneyScript = GameObject.FindGameObjectWithTag("Money").GetComponent<Money>();
    }

    public void OnClick()
    {   
        if (MoneyScript.Enough == true)
        {
            //ChestInv.slots = new GameObject[2];

            Slot1.SetActive(true);

            //Slot2.SetActive(true);

            Slot.GetComponent<Money>().UpBuy();

            //Money -= 40f;

            //MoneyText.text = "Money: $ " + Money;

            Destroy(Placeholder);

            Upgrader.interactable = false;
        }
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}*/
